CoffeeCup
=========

This is a sample to use React Native App with Rails backend.

To start the server:

```
cd server
bundle install
rake db:reset
rails server
```

To run the app:

```
open ios/CoffeeCup.xcodeproj
```
Click Run
